div(class="search-menu  flex-row ")
    if(users && users.length)
        each user in users 
            a(class="d-flex flex-row align-items-center search-menu-item" href="/users/"+ user.userName)
                img(width="40" height="40" class="p-1" src=user.avatar)
                span(class="pl-3")= user.userName 
    else 
        span(class="p-3") Aucun Résultat